# 版本切换说明（临时版 / 正常版）

本项目中涉及 3 个文件，同时保留了「正常运行的版本」和「临时占位版本」两套代码。
**当前默认启用的是「正常运行的版本」**，临时版本代码均以注释形式保留在文件中，方便日后切换。

涉及文件：

| 文件 | 正常版本 | 临时版本 |
| ---- | -------- | -------- |
| `src/permission.ts` | 完整路由守卫（权限校验 + 动态路由） | 简化守卫（仅校验 token） |
| `src/views/home/index.vue` | 正常首页（公告/留言/快捷入口等） | 首页强制跳转 `/login` |
| `src/views/login/index.vue` | 完整登录页（账号/扫码/注册/找回密码） | 星空占位页 |

---

## 一、切换为「临时版本」

### 1. `src/permission.ts`
- 注释掉文件顶部的 `router.beforeEach(async to => { ... })`（即当前的正常逻辑）。
- 取消文件底部 `临时版本，已注释` 代码块中 `router.beforeEach` 的每一行注释。

### 2. `src/views/home/index.vue`
- 在 script 区找到这三处带 `【临时版本】` 注释的行，取消它们的注释：
  1. `import { useRouter } from 'vue-router'`
  2. `const router = useRouter()`
  3. `onMounted(() => { router.push('/login') })`

### 3. `src/views/login/index.vue`
- 将文件上方（当前启用的）`<template>`、`<script setup>`、`<style scoped>` 三个区块整体注释掉。
- 取消文件底部 `临时版本，已注释` 处以 `<!--` 开始、`-->` 结束的星空占位页的注释。

---

## 二、切换回「正常运行版本」（默认）

将上述所有步骤反过来执行即可：
- 取消正常版本代码的注释，恢复为可运行状态；
- 把临时版本代码再次注释掉。

切换完成后建议重新运行开发服务器：`npm run dev`，并刷新浏览器验证。

---

## 三、注意事项
- 临时版本主要用于应急/占位展示，**不具备完整的登录与权限校验能力**，切换后相关业务功能（登录、动态路由、权限控制）会失效。
- 切换时请确保同一文件的正常 / 临时代码**只启用其中一套**，不要同时启用，否则会出现重复定义或逻辑冲突。
- 若需彻底删除临时版本，直接删除对应注释代码块即可。